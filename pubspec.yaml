name: algorithm_preveiw
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2  # 아이콘 패키지

  provider: ^6.0.0         # 상태 관리 패키지
  charts_flutter: ^0.12.0 # 차트 시각화를 위한 패키지

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true  # Material Icons 사용 설정

  # 만약 이미지나 폰트를 추가하고 싶다면 아래와 같은 형식으로 작성
  # assets:
  #   - images/a_dot_burr.jpeg
  # fonts:
  #   - family: CustomFont
  #     fonts:
  #       - asset: fonts/CustomFont-Regular.ttf
